const ItemListContainer = ({listarItem}) => {
  return (
    <div>
        <li>{listarItem}</li>
    </div>
  )
}

export default ItemListContainer;

// esto es provisional, cuando tenga los productos cargados voy a cambiar el componente para que me lo liste bien